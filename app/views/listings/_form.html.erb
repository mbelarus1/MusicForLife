<%= simple_form_for(listing) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :photo, as: :file %>
  <%= f.submit 'Create', class: 'btn btn-block btn-primary'%>
<% end %>
